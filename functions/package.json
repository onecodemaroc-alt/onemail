{
  "name": "onemail-functions",
  "scripts": {
    "build": "cp -r ../backend/src/* ./src/",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.2.0",
    "firebase-functions": "^6.3.2",
    "nodemailer": "^6.10.0"
  }
}
